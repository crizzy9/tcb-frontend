// const ENV = process.env.APP_ENV || 'dev';

// function loadConfig(env) {
//     try {
//         return require('../environments' + env);
//     } catch (e) {
//         console.log('Could not load environment configuration file' + env + '.js');
//         process.exit(0);
//         return {};
//     }
// }

// export const config = loadConfig(ENV);
